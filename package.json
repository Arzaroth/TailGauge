{
  "name": "tailgauge",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "description": "Tailscale in the panel, for GNOME Shell, KDE Plasma and Omarchy.",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "scripts/build.sh",
    "typecheck": "tsc -p tsconfig.gnome.json --noEmit",
    "test": "scripts/build.sh && node --import ./tests/gnome/register.js --test 'tests/gnome/**/*.test.js'"
  },
  "devDependencies": {
    "@girs/gnome-shell": "50.0.4",
    "@types/node": "^22.10.2",
    "typescript": "~5.7.2"
  }
}
